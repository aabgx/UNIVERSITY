# incercam sa nu folosim spatiu in plus?
# fac ca si la prolog, daca e lista merg pe fiecare element, daca nu le inmultesc
def prod_scalar(v1, v2):
    if isinstance(v1, list):
        n = len(v1)
        suma = 0
        for i in range(n):
            suma += prod_scalar(v1[i], v2[i])
        return suma
    return v1 * v2


print(prod_scalar([1, 0, 2, 0, 3], [1, 2, 0, 3, 1]))

assert prod_scalar([1, 0, 2, 0, 3], [1, 2, 0, 3, 1]) == 4
